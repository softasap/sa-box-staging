---
# defaults file for sa-box-staging

  role_dir: "{{ lookup('pipe','pwd')|dirname }}"

  option_install_webmin: true
  option_install_webmin_prerequisites: false

  option_install_mailhog: true

  option_install_xdebug: false

  mailhog_version: 0.1.6

  php_xdebug_version: 2.2.4
  apache_mode: worker # use prefork or worker variables
